export default function CreateService() {
    return (
        <div>
            {
                // add button to create new service
                // allow user to search for songs
            }
        </div>
    )
}